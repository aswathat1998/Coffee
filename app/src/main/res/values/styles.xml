<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/coffeeDark</item>
        <item name="colorPrimaryDark">@color/coffeeDarkest</item>
        <item name="colorAccent">@color/coffeeLight</item>
    </style>
    <style name="EditTextTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/coffeeLight</item>
        <item name="colorControlActivated">@color/coffeeDark</item>
        <item name="colorControlHighlight">@color/coffeeDarkest</item>
    </style>
</resources>
